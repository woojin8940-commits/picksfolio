/**
 * 기본 버튼(카카오톡 · 유튜브 · 틱톡 · 네이버)의 플랫폼 로고.
 *
 * 기본 버튼은 이름만 글자로 나왔다. 그런데 이 네 개는 이름을 읽기 전에 로고로
 * 알아보는 것들이고, 글자만 있으면 인플루언서가 직접 만든 커스텀 버튼과 생김새가
 * 똑같아진다 — 그러면 플랫폼이 이름·주소 형식을 정해 두는 의미가 없어진다.
 *
 * 로고는 각 플랫폼이 쓰는 앱 아이콘 형태(브랜드색 사각 타일 + 대비되는 마크)로
 * 그린다. 이미지 파일을 두지 않고 인라인 SVG 로 넣는 이유는 두 가지다: 버튼 하나에
 * 요청이 하나씩 더 붙지 않고, 밝은 테마와 어두운 테마 어디에 놓아도 타일이 배경을
 * 가지고 있어 마크가 배경색에 묻히지 않는다(카카오 노랑 바탕의 검은 말풍선, 유튜브
 * 흰 바탕의 빨간 마크처럼 각 로고는 정해진 바탕색과 함께 써야 알아볼 수 있다).
 *
 * 버튼 자체의 색은 여전히 테마색이다 — 타일만 브랜드색이라 버튼 줄이 알록달록해지지
 * 않고, 강조색은 비즈니스 제안 버튼이 계속 독점한다.
 */

import React from 'react';
import type { DefaultButtonKey } from '../utils/pageButtons';

type LogoMark = {
  /** 타일 배경 = 그 로고를 쓸 때 정해진 바탕색. */
  bg: string;
  /** 마크 색. */
  fg: string;
  /** 타일 대비 마크 크기. 로고마다 여백이 다르게 그려져 있어 값이 다르다. */
  scale: number;
  /** 24×24 기준 경로. */
  path: string;
};

const MARKS: Record<DefaultButtonKey, LogoMark> = {
  kakao: {
    bg: '#FEE500',
    fg: '#000000',
    scale: 0.72,
    path: 'M12 3C6.48 3 2 6.36 2 10.44c0 2.62 1.72 4.92 4.32 6.24-.14.52-.92 3.36-.96 3.58 0 0-.02.16.08.22.1.06.22.02.22.02.3-.04 3.44-2.26 3.98-2.64.76.1 1.56.16 2.36.16 5.52 0 10-3.36 10-7.58C22 6.36 17.52 3 12 3z',
  },
  youtube: {
    bg: '#FFFFFF',
    fg: '#FF0000',
    scale: 0.76,
    path: 'M23.498 6.186a3.016 3.016 0 0 0-2.122-2.136C19.505 3.545 12 3.545 12 3.545s-7.505 0-9.377.505A3.017 3.017 0 0 0 .502 6.186C0 8.07 0 12 0 12s0 3.93.502 5.814a3.016 3.016 0 0 0 2.122 2.136c1.871.505 9.376.505 9.376.505s7.505 0 9.377-.505a3.015 3.015 0 0 0 2.122-2.136C24 15.93 24 12 24 12s0-3.93-.502-5.814zM9.545 15.568V8.432L15.818 12l-6.273 3.568z',
  },
  tiktok: {
    bg: '#000000',
    fg: '#FFFFFF',
    scale: 0.62,
    path: 'M12.525.02c1.31-.02 2.61-.01 3.91-.02.08 1.53.63 3.09 1.75 4.17 1.12 1.11 2.7 1.62 4.24 1.79v4.03c-1.44-.05-2.89-.35-4.2-.97-.57-.26-1.1-.59-1.62-.93-.01 2.92.01 5.84-.02 8.75-.08 1.4-.54 2.79-1.35 3.94-1.31 1.92-3.58 3.17-5.91 3.21-1.43.08-2.86-.31-4.08-1.03-2.02-1.19-3.44-3.37-3.65-5.71-.02-.5-.03-1-.01-1.49.18-1.9 1.12-3.72 2.58-4.96 1.66-1.44 3.98-2.13 6.15-1.72.02 1.48-.04 2.96-.04 4.44-.99-.32-2.15-.23-3.02.37-.63.41-1.11 1.04-1.36 1.75-.21.51-.15 1.07-.14 1.61.24 1.64 1.82 3.02 3.5 2.87 1.12-.01 2.19-.66 2.77-1.61.19-.33.4-.67.41-1.06.1-1.79.06-3.57.07-5.36.01-4.03-.01-8.05.02-12.07z',
  },
  naver: {
    bg: '#03C75A',
    fg: '#FFFFFF',
    scale: 0.5,
    path: 'M16.273 12.845 7.376 0H0v24h7.726V11.156L16.624 24H24V0h-7.727v12.845Z',
  },
};

/**
 * 로고 하나. `size` 는 타일 한 변(px)이다.
 *
 * 아이콘일 뿐이므로 이름을 읽어 주지 않는다(aria-hidden) — 로고 옆에는 늘 버튼
 * 이름이 글자로 함께 있어서, 스크린리더가 "카카오톡 카카오톡" 으로 두 번 읽는 것을
 * 막는다.
 */
export const PlatformLogo: React.FC<{
  platform: DefaultButtonKey;
  size?: number;
  className?: string;
}> = ({ platform, size = 16, className = '' }) => {
  const mark = MARKS[platform];
  if (!mark) return null;
  const inner = Math.round(size * mark.scale * 100) / 100;
  return (
    <span
      aria-hidden="true"
      className={`inline-flex items-center justify-center shrink-0 overflow-hidden ${className}`}
      style={{
        width: size,
        height: size,
        borderRadius: Math.max(2, Math.round(size * 0.28)),
        backgroundColor: mark.bg,
      }}
    >
      <svg width={inner} height={inner} viewBox="0 0 24 24" fill={mark.fg} focusable="false">
        <path d={mark.path} />
      </svg>
    </span>
  );
};

export default PlatformLogo;
