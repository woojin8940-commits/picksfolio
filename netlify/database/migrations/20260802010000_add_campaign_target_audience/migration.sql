-- 타겟 오디언스 — "이 제품을 쓸 사람"을 브랜드 말로 한 줄.
--
-- 희망 인플루언서 조건(성별·연령·팔로워 규모)은 후보를 걸러내는 값이지만, 그것만으로는
-- 담당자가 왜 그 조건인지 알 수 없다. "민감성 피부로 고민하는 20대 후반 직장인"처럼
-- 브랜드가 떠올리는 사람을 그대로 받아 두면, 조건에 딱 맞지 않아도 더 잘 맞는 후보를
-- 담당자가 제안할 수 있다.
--
-- requirements 에 문장으로 합쳐 넣을 수도 있었지만, requirements 는 조건 칩에서 매번
-- 다시 생성되는 값이다. 거기에 섞으면 캠페인을 수정할 때마다 이 문장이 지워진다.

ALTER TABLE campaigns
  ADD COLUMN IF NOT EXISTS target_audience TEXT DEFAULT '';
