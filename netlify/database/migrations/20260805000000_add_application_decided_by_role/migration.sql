-- 지원자 결정을 누가 내렸는지 기록한다.
--
-- 제품 협찬형·공동구매는 브랜드가 지원자 중에서 직접 수락하고, 광고비 지급형은
-- 픽스폴리오 담당자가 고른다. decided_by 에는 누른 사람의 아이디만 남는데,
-- 담당자 아이디와 브랜드 계정 아이디는 형태가 같아서 값만 보고는 어느 쪽인지
-- 알 수 없다. 협업이 왜 시작됐는지를 나중에 되짚으려면 역할이 따로 있어야 한다.
--
-- 기존 행은 전부 담당자 결정이었지만 빈 문자열로 둔다 — 소급해서 'manager' 로
-- 채우면 "그때는 담당자만 결정할 수 있었다"는 사실과 "그렇게 기록했다"는 사실이
-- 구분되지 않는다.
ALTER TABLE campaign_applications ADD COLUMN IF NOT EXISTS decided_by_role TEXT DEFAULT '';
