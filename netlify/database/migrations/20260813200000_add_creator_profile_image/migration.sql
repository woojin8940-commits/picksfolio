-- 인플루언서 채널의 프로필 사진.
--
-- 지금까지 후보 카드의 동그란 프로필 자리는 site_data.profile.avatar_url 한 곳에서만
-- 왔다. 그 값은 픽스폴리오 안에서 본인이 따로 올린 사진이라, 인스타 연동만 하고
-- 페이지를 안 꾸민 사람은 이 자리가 영영 비어 있었다. 브랜드 화면에서는 그게 카드
-- 절반이 회색 동그라미인 명단으로 보이고, 얼굴 없는 후보는 아무리 숫자가 좋아도
-- 클릭을 못 받는다.
--
-- 그래서 메타 동기화 때 함께 받아 오는 프로필 사진을 채널 행에 둔다. 채널 지표와
-- 같은 시점에 같은 계정에서 온 값이라야, 계정을 갈아 끼웠을 때 사진만 옛 계정으로
-- 남는 일이 없다(persistMetrics 의 sameAccount 규칙을 그대로 탄다).
--
-- 메타가 주는 주소는 만료된다. 릴스·피드 썸네일과 같은 성질이라 화면도 같은 방식으로
-- 처리한다 — 깨진 자리를 회색 동그라미로 두고, 다음 동기화가 새 주소로 덮어쓴다.
-- 비어 있으면 예전처럼 site_data 의 아바타로 되돌아간다.

ALTER TABLE creator_channels
  ADD COLUMN IF NOT EXISTS profile_image TEXT DEFAULT '';
