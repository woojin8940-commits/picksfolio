-- 가이드라인을 "파일"로 올려 둘 자리.
--
-- 지금까지 가이드라인은 텍스트(guideline_note)와 링크(guideline_url) 두 칸뿐이었다.
-- 그런데 브랜드가 실제로 가진 것은 PDF·PPT·이미지 파일이다. 링크 칸에 넣으려면
-- 어딘가에 먼저 올려 공유 링크를 만들어야 했고, 그 과정이 귀찮아서 대부분은
-- 카카오톡으로 파일을 보내고 이 칸은 비워 두었다. 그래서 인플루언서 진행 화면에는
-- "아직 전달된 가이드라인이 없습니다"만 남았다.
--
-- 파일을 여러 개 받는 이유는 가이드가 한 장이 아니기 때문이다 — 브랜드 톤앤매너,
-- 제품 상세, 필수 문구 예시가 각각 다른 파일로 온다. 하나만 받으면 나머지는
-- 결국 대화방으로 흩어진다.
--
-- 저장 형태: [{ "url": "...", "name": "가이드.pdf", "mimeType": "application/pdf",
--              "uploadedAt": "2026-08-13T...", "uploadedBy": "acme" }]
-- 파일 본체는 기존 업로드 스토어(/api/upload-image)에 그대로 두고 여기에는 주소만
-- 적는다. 캠페인 단위로 두는 것은 가이드라인이 인플루언서마다 다르지 않기
-- 때문이다 — 한 번 올리면 그 캠페인에 확정된 모든 인플루언서가 같은 것을 본다.

ALTER TABLE campaigns
  ADD COLUMN IF NOT EXISTS guideline_files JSONB DEFAULT '[]'::jsonb;
